package io.github.kilgoret.mate

/**
 * Корневой маркер эффекта — разового РАЗЛИЧИМОГО намерения, которое
 * reducer описывает данными, а runtime доставляет исполнителю.
 *
 * Принцип П1 («различимые намерения»): в результате одного reduce
 * эффекты образуют МНОЖЕСТВО — неразличимый дубль намерения не имеет
 * семантики. Если кажется, что нужно «два одинаковых» эффекта, — они
 * различимы по смыслу, и эта различимость обязана жить в данных
 * эффекта (цель, причина, порядковый номер, idempotency-ключ).
 *
 * > «У mate нет двух одинаковых причин его пить».
 */
public interface Effect
